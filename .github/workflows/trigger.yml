name: Trigger

on:
  push:
  
  
jobs:
  TriggerProd:
    if: github.ref == 'refs/heads/main'
    uses: jan-tricks/workflows/.github/workflows/deploy.yml@latest
    with:
      env: "Production"
    secrets: inherit
    
  TriggerStage:
    if: github.ref == 'refs/heads/develop'
    uses: jan-tricks/workflows/.github/workflows/deploy.yml@latest
    with:
      env: "Stage"
    secrets: inherit
