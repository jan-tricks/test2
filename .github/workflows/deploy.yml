name: Deployment

on:
  workflow_call:
    inputs:
      username:
        description: 'A username passed from the caller workflow'
        default: 'john-doe'
        required: true
        type: string

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build ${{ inputs.username }}"
